gradio>=5.31.0,<6.0.0
# >=0.30: InferenceClient.summarization renamed its `parameters` kwarg to
# `generate_parameters` in 0.30. The old floor of 0.25 let a Space rebuild
# resolve to a version whose signature the code did not match.
huggingface_hub>=0.30.0,<1.0.0
PyMuPDF>=1.24.0,<2.0.0
